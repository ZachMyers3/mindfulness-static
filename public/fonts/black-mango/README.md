# Black Mango webfonts

Font files live in `src/assets/fonts/black-mango/` so Astro/Vite can hash them and respect `base` (GitHub Pages).

Do not put `.woff2` / `.ttf` files in this `public/` folder.
